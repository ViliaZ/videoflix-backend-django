from django.urls import path
from . import views
# deactivated for now, views are called via router in Main App

urlpatterns = [
#  path('users/', views.UserListCreateView.as_view(), name='user-list-create'),
]
